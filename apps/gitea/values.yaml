ingress:
  enabled: true
  className: nginx
  annotations:
    nginx.ingress.kubernetes.io/rewright-target: /
  hosts:
    - host: gitea.local
      paths:
        - path: /
          pathType: Prefix

persistence:
  enabled: true
  size: 10Gi
  storageClass: standard

postgresql:
  enabled: true
  auth:
    postgresPassword: gitea
    username: gitea
    password: gitea
    database: gitea